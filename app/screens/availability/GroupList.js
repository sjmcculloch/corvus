import React from 'react';
import { View } from 'react-native';
import { connect } from 'react-redux';

const GroupList = () => <View />;

const mapStateToProps = () => ({});

export default connect(mapStateToProps)(GroupList);
